(function(){
    // example config for corporate

    var app = window.app;

    app('app-config-mixin', {
        // some redefined keys


        // example
        server: {
            enable: false
        }

    });

})();